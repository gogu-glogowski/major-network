#!/usr/bin/env bash
# Fail-closed lab client: Nitrokey PIN + touch on PROOF.
set -euo pipefail
cd "$(dirname "$0")/.."
exec cargo run -p mnp-client -- '[::1]:4433' mnp-lab-cert.der mnp-lab-client.id mnp-lab-server.id
