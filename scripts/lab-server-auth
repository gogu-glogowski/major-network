#!/usr/bin/env bash
# Fail-closed lab server: pin client's announce (mnp-lab-client.id).
set -euo pipefail
cd "$(dirname "$0")/.."
exec cargo run -p mnp-server -- '[::1]:4433' mnp-lab-cert.der mnp-lab-server.id mnp-lab-client.id
